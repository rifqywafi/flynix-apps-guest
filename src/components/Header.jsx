export default function Header({title}){
    return(
        <div className="pb-4 font-volkhov text-primary flex justify-start">
            <p className="font-bold text-4xl">{title}</p>
        </div>
    )   
}